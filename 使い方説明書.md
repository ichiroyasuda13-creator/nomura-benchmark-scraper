# ファンド・ベンチマーク抽出 & 営業インテリジェンス 使い方説明書 (Enterprise Edition)

この説明書は、開発者でない方（営業・企画担当）が読んでも全体像がわかり、
実際に動かす人や Cursor にそのまま渡せることを目指しています。

---

## 1. このアプリは何をするもの？（1分でわかる概要）

**ひとことで言うと**：野村アセットマネジメントの投資信託について、「その商品がどの指数（ベンチマーク）を使っているか」を、公式PDF（交付目論見書）から自動で読み取って、MSCI営業ターゲット分析と一覧表（Excel/CSV）にするアプリです。

**なぜ必要か**：MSCIの営業では、「純資産（AUM）が大きいのに、まだMSCI以外の指数を使っているファンド」を見つけることが重要です。数百本のファンドのPDFを人が1本ずつ開いて確認するのは大変なので、その作業を自動化します。

**何が出てくるか**：
1. **多機能Excel (`nomura_benchmarks.xlsx`)**:
   - `ファンド一覧`：全ファンドのベンチマーク・提供会社・信頼度・確認状況（ゼブラ柄＆数値書式済み）
   - `MSCI営業ターゲット`：他社指数を採用している高AUMファンドの優先順位リスト
   - `指数提供者別サマリー`：指数会社ごとの純資産シェア（兆円・構成比%）
2. **直感的なWebアプリ & Streamlitダッシュボード**:
   - ブラウザ上でチャート分析、手動編集、単体再抽出が可能。

```
 【入力】野村の公式サイト（AUM上位最大500本）
    ↓  自動でPDFを取得してマルチLLM(Claude/Gemini/GPT)で高精度読み取り
 【出力】多機能Excel (3シート) / CSV / インタラクティブWeb画面
    ┌──────┬────────┬──────────┬────────┬────────┐
    │ファンド名 │純資産   │ベンチマーク  │提供会社  │MSCI？   │
    ├──────┼────────┼──────────┼────────┼────────┤
    │○○ファンド │1兆円    │MSCIコクサイ │MSCI    │はい     │
    │△△ETF    │3兆円    │TOPIX      │JPX/東証 │いいえ   │ ← 優先営業ターゲット🎯
    └──────┴────────┴──────────┴────────┴────────┘
```

---

## 2. アプリの処理の流れ（中で何をしているか）

技術がわからなくても、この6段階だけ知っておけば十分です。

1. **一覧を取る**：公式サイトから、純資産の大きい順に指定本数のファンド情報を取得
2. **PDFの場所を探す**：各ファンドの「交付目論見書」PDFのURLを並行解決（高速化）
3. **PDFを保存**：そのPDFを並行ダウンロード
4. **文字にする**：PDFから文章を取り出す（必要時はOCRで画像読み取り）
5. **指数を読み取る**：Claude / Gemini / GPT / Ollama などのマルチAIとルールベースで指数名を高精度特定
6. **表にする**：3シート構成の多機能ExcelおよびCSVを出力

途中で中断してもキャッシュされるため、**再度実行すれば続きから高速に再開**できます。

---

## 3. 動かす前に用意するもの（前提）

- **Python 3.11 以上**
- **必要な部品（ライブラリ）**：`requirements.txt` に記載
- **LLM APIキー（いずれか1つ以上でOK）**：
  - `ANTHROPIC_API_KEY`（Claude 3.5 Sonnet / Claude 3.7）
  - `GEMINI_API_KEY`（Google Gemini 2.0 Flash / Pro）
  - `OPENAI_API_KEY`（OpenAI GPT-4o / GPT-4o-mini）
  - ※未設定の場合は完全ルールベースで動作します。


---

## 4. セットアップ手順（初回だけ）

```bash
# 1. プロジェクトのフォルダに移動
cd nomura-benchmark-scraper

# 2. 必要な部品をインストール
pip install -r requirements.txt

# 3. ブラウザ部品を入れる（Playwrightを使う場合）
playwright install chromium

# 4. APIキーを設定（.env.example をコピーして .env を作り、キーを記入）
cp .env.example .env
#   .env を開いて ANTHROPIC_API_KEY=... を記入
```

Tesseract と poppler は OS ごとに入れ方が異なります（例：Mac は `brew install tesseract tesseract-lang poppler`）。詳細は `README.md` を参照してください。

---

## 5. 基本の使い方（実行のしかた）

### いちばん簡単な方法（全部いっぺんに）
```bash
python -m app.run
```
これだけで「一覧取得 → PDF取得 → 文字化 → 指数抽出 → 表出力」まで通しで実行され、
最後に `output/nomura_benchmarks.xlsx` と `.csv` ができます。

### 段階ごとに実行したいとき（デバッグや部分的なやり直し）
```bash
python -m app.stage1_list          # 一覧だけ取得
python -m app.stage2_pdf_url       # PDFのURLだけ取得
python -m app.stage3_download      # PDFのダウンロードだけ
python -m app.stage4_extract_text  # 文字化だけ
python -m app.stage5_benchmark     # 指数の読み取りだけ
python -m app.stage6_output        # 表の出力だけ
```

### 対象の本数を変えたいとき
上位100本ではなく50本にしたい、などは設定ファイル（または起動時のオプション）で指定します。
実装に合わせて `README.md` の「設定」欄を確認してください。

---

## 6. 出力ファイル（表）の見方

`output/nomura_benchmarks.xlsx` の主な列と、営業での使い方です。

| 列名 | 意味 | 営業での見方 |
|---|---|---|
| `rank` | 純資産の順位 | 上位ほど影響が大きい |
| `fund_name` | ファンド名 | — |
| `aum` | 純資産総額 | 大きいほど重要顧客 |
| `fund_type` | インデックス型／アクティブ型／バランス型 など | — |
| `benchmark` | 使っている指数の名前 | — |
| `index_provider` | 指数の提供会社（MSCI/東証/日経/野村など） | — |
| **`is_msci`** | **MSCIの指数を使っているか（はい/いいえ）** | **営業判断の中心。ここが「いいえ」で純資産が大きい＝置換提案の狙い目** |
| `confidence` | 読み取りの確からしさ（high/medium/low） | low は要確認 |
| **`needs_review`** | **人の目で確認したほうがよい行の印** | **ここが「はい」の行は営業に使う前にチェック** |
| `prospectus_pdf_url` | 根拠にしたPDFのURL | 確認したいときに開く |
| `note` | 補足・不明の理由など | — |

**使い方のコツ**：`is_msci = いいえ` かつ `aum` が大きい行を上から見ていくと、「MSCIに乗り換えてもらう価値が高いファンド」が並びます。ただし `needs_review = はい` の行は、営業に使う前に必ず根拠PDFで確認してください（自動読み取りが不確実なため）。

---

## 7. うまくいかないとき（トラブルシューティング）

### 「ベンチマークが不明」の行が多い
いちばん多い症状です。原因は主に2つ。切り分け方は簡単です。

まず、不明になったファンドについて、文字化したファイル（`data/text/{ファンドコード}.txt`）を開き、指数名（例：MSCI、TOPIX、日経）が文章の中にあるか探します。

- **指数名が文章の中にある** のに不明 → 「読み取りルールの問題」。`benchmark_extraction_improvement.md` の §2・§3（トリガー語の拡張と指数名の切り出し）を適用します。
- **指数名が文章の中にない** → 「文字化けやOCRの問題」。同ファイルの §5（OCRの強制）を適用します。

とくに、インデックスファンドは「ベンチマーク」という言葉を使わず「**○○に連動する投資成果をめざします**」と書くため、読み取り条件を「連動」系まで広げるのが最大の改善策です。詳しくは改善版ドキュメントを参照してください。

### PDFがダウンロードできない／URLが違う
野村のPDFのURLの作り方が変わった可能性があります。ファンド詳細ページを実際に開き、「交付目論見書」リンクの本当のURLを確認して、Stage 2 の取得方法を合わせます。

### サイトから一覧が取れない
野村の検索ページはページを開いた後に中身が表示される仕組みのため、ページの裏で動いているデータ取得の通信（API）を調べると安定します。仕様書 `nomura_benchmark_scraper_spec.md` の §4（最優先の調査タスク）を参照してください。

---

## 8. 他の運用会社・ファンドに応用する

このアプリは「野村専用」の部分と「どの運用会社でも使い回せる」部分に分かれています。応用のコツは、**使い回せる部分はそのまま、会社ごとに違う部分だけ作り替える**ことです。

### 作り替えが必要な部分（会社ごとに違う）
- **一覧の取り方**（サイトの構造が会社ごとに違う）＝ Stage 1
- **PDFの場所の探し方**（URLの作り方が違う）＝ Stage 2

### そのまま使い回せる部分（共通）
- **PDFの文字化・OCR** ＝ Stage 4
- **指数の読み取りロジック**（「連動」「ベンチマーク」の探し方、指数→提供会社の対応表、ファンド名からの推定）＝ Stage 5
- **表の出力** ＝ Stage 6

つまり、大和アセットや三菱UFJアセットなどに広げる場合も、**指数を読み取る心臓部（Stage 4〜6）はほぼ流用でき、サイトからの取得（Stage 1〜2）だけ作り直せばよい**、というのが基本です。

---

### 8-1. Claude（このチャット）への指示の出し方

Claude には「設計・調査・改善」を頼むのが得意分野です。以下をコピーして、会社名などを書き換えて使ってください。

**【テンプレA：別の運用会社版の仕様書を作ってもらう】**
```
野村アセットマネジメント向けに作ったファンド・ベンチマーク抽出アプリを、
「大和アセットマネジメント」向けに応用したいです。

・添付の仕様書(nomura_benchmark_scraper_spec.md)と
 改善版ロジック(benchmark_extraction_improvement.md)をベースにしてください。
・指数の読み取り部分(Stage 4〜6)は流用できる前提で、
 サイトからの一覧取得(Stage 1)とPDF URL取得(Stage 2)を
 大和アセット向けに作り替えるための指示書を作ってください。
・大和アセットのファンド検索ページのURLはこちらです：<URLを貼る>
・出力の形式(列)は野村版と揃えてください。
```

**【テンプレB：不明が多い問題を直してもらう】**
```
ベンチマークが「不明」になるファンドが多いです。改善してください。
・不明になった代表20件のファンド名と、文字化したテキスト(data/text/*.txt)を貼ります：
 <ここに貼る>
・どの読み取りルールを追加・修正すればよいか、正規表現とLLMプロンプトの
 具体案を出してください。
```

**【テンプレC：出力結果を営業向けに整理してもらう】**
```
出力されたnomura_benchmarks.csvを貼ります。
・is_msciが「いいえ」かつ純資産が大きいファンドを上位20件、
 「MSCI置換提案の優先リスト」として表にまとめてください。
・各ファンドについて、現在使っている指数と、提案できるMSCI指数の候補も添えてください。
```

コツ：Claude に頼むときは、**関連ファイルを添付し、「何を流用し、何を作り替えるか」を明示**すると精度が上がります。

---

### 8-2. Cursor への指示の出し方

Cursor には「実際のコードを書く・直す」ことを頼みます。仕様書と改善版ドキュメントをプロジェクトに置いた状態で、以下のように指示します。

**【テンプレD：新しい運用会社に対応させる】**
```
このリポジトリは野村アセット向けのファンド・ベンチマーク抽出アプリです。
仕様は nomura_benchmark_scraper_spec.md、抽出改善は
benchmark_extraction_improvement.md にあります。

これを「大和アセットマネジメント」に対応させてください。
1. Stage 4〜6（テキスト抽出・ベンチマーク抽出・出力）は原則そのまま流用。
2. Stage 1（一覧取得）を大和アセットのファンド検索ページ用に作り直す。
   ・まずブラウザのDevToolsでデータ取得APIがないか調べ、あればそれを使う。
   ・対象URL: <URLを貼る>
   ・純資産の降順で上位100本を funds.json に出す。
3. Stage 2（交付目論見書PDFのURL取得）を大和向けに作り直す。
   ・詳細ページのHTMLから「交付目論見書」リンクを厳密に判定して取得。
4. 会社ごとの違いは設定ファイル（config）に切り出し、
   1つのコードベースで会社を切り替えられる構造にリファクタしてください。
出力の列は野村版と同じスキーマを維持すること。
```

**【テンプレE：抽出の不明を減らす改修】**
```
benchmark_extraction_improvement.md の方針に沿って、Stage 5 を改修してください。
特に：
・トリガー語を「連動する投資成果をめざす」「対象インデックス」等まで拡張（§2）
・指数名の括弧修飾（配当込み等）を落とさない切り出し（§3）
・ファンド名からのフォールバック抽出を追加（§4）
・インデックス型で本文から取れない場合はOCRを強制（§5）
改修後、§9のテストケースが「不明」にならないことを確認してください。
```

**【テンプレF：まず小さく試す】**
```
いきなり100本ではなく、まず上位5本だけで通しで動かせるようにしてください。
各Stageの結果（funds.json、PDF、text、抽出結果）を確認できるログを出し、
5本が正しく取れることを確認してから本数を増やせる作りにしてください。
```

コツ：Cursor には**一度に全部ではなく、Stageごと・少数本から**頼むと失敗が減ります。仕様書と改善版をリポジトリに入れて「これに従って」と指示するのが最も確実です。

---

## 9. 用語集（誰でもわかるように）

- **AUM（純資産総額）**：そのファンドに集まっているお金の総額。大きいほど影響力・重要度が高い。
- **ベンチマーク／指数**：ファンドが目標や基準にする「ものさし」。例：TOPIX、日経225、MSCIコクサイ。
- **インデックス型（パッシブ）**：指数と同じ値動きをめざすファンド。「○○に連動」と書かれる。
- **アクティブ型**：指数を上回ることをめざすファンド。「○○をベンチマークとする」等。
- **交付目論見書**：投資信託の公式な説明書（PDF）。ベンチマークはここに書かれている。
- **OCR**：画像になっている文字を読み取って文章に変換する技術。
- **API**：プログラム同士がデータをやり取りする窓口。サイトの裏側でデータを返していることがある。
- **指数プロバイダー**：指数を作って提供している会社。MSCI、東証（JPX）、日経、野村など。
- **confidence（確からしさ）**：自動読み取りの信頼度。low は人の確認推奨。
- **needs_review**：人の目で確認したほうがよい、という印。

---

### 困ったときは
- 設計や調査、結果の整理 → **Claude**（このチャット）に、関連ファイルを添付して相談
- 実際のコードの作成・修正 → **Cursor**に、仕様書と改善版を渡して依頼
- 不明が多いなど品質の問題 → まず `data/text/` を確認し、`benchmark_extraction_improvement.md` の §10（デバッグの勘所）へ
