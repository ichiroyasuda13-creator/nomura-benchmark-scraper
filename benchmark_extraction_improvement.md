# 【改善版】ベンチマーク抽出ロジック（§7 差し替え／追補）

> 症状：**ベンチマークが「不明」になるファンドが多い（特にインデックス型）。**
> 原因と対策を以下にまとめる。既存の Stage 5（`stage5_benchmark.py`）を本書の方針で置き換える。

---

## 1. 根本原因（最重要）

**インデックスファンドは目論見書で「ベンチマーク」という語をほとんど使わない。**

- インデックス型の定型文は「**○○に連動する投資成果をめざします**」「**○○の動きに連動する**」であり、"ベンチマーク" という単語は出てこない。
- "ベンチマーク" はアクティブ運用で「上回ることをめざす基準」として使う語。
- → 抽出トリガーを "ベンチマーク" に依存させていると、**純インデックスファンドが全部「不明」に落ちる**。これが「不明が多い」の主因。

対策の柱は3つ：
1. トリガー語を「連動」系まで大幅に広げる（§2）
2. **ファンド名からのフォールバック抽出**を必ず入れる（§4）— これだけで大量に救える
3. インデックス型なのに取れない場合は**OCRを強制**する（§5）

---

## 2. トリガーパターンの拡張

「ベンチマーク」だけでなく、以下の**連動系・対象指数系**を全てトリガーにする。

### 連動をめざす系（インデックス型の主流）
- `…に連動する投資成果をめざ(す|します)`
- `…に連動する投資成果を目指`
- `…に連動する運用成果をめざ`
- `…（の動き）に連動する`
- `…への連動をめざ`
- `…に連動させることを(目的|めざ)`

### 対象指数の定義系
- `…（以下「対象インデックス」といいます）`
- `…（以下「対象指数」といいます）`
- `対象インデックス[はとは：:]{0,3}…`
- `対象指数[はとは：:]{0,3}…`

### ベンチマーク系（アクティブ／一部インデックス）
- `…をベンチマーク[とする|とします|とし]`
- `ベンチマーク[はとする：:]{0,3}…`

### 否定（BMなしの明示）
- `ベンチマークは(設けません|設定していません|ありません)`
- `特定の指数を(ベンチマーク|運用の目標)とはしません`
→ ヒットしたら `benchmark="なし"`, `fund_type` はアクティブ系に。

---

## 3. 指数名の正しい切り出し（parenthetical を落とさない）

指数名は末尾に `（配当込み）`「（円換算ベース）」「（円ベース）」「（税引後配当込み）」等の**全角括弧の修飾が連続**する。ここを含めて1つの指数名として取る。切りすぎ・取りすぎの両方に注意。

### 実装方針（正規表現の種。実データで必ず調整）
```python
import re

# 指数名候補：連動/対象/ベンチマークの直前に来る名詞句を、全角括弧の連続まで含めて取る
# 例: 「MSCI エマージング・マーケット・インデックス（配当込み、円換算ベース）に連動する投資成果をめざします」
IDX_CHARS = r"[0-9A-Za-zＡ-Ｚａ-ｚ０-９ぁ-んァ-ヶ一-龠ー・／/\-\.&＆　 （）\(\),、，]"

PATTERNS = [
    # 連動系（前方に指数名）
    re.compile(rf"({IDX_CHARS}{{2,80}}?)(?:の動き)?に連動する(?:投資|運用)成果を(?:めざ|目指)"),
    re.compile(rf"({IDX_CHARS}{{2,80}}?)(?:の動き)?に連動(?:させる|することを)"),
    # 対象インデックス/対象指数の定義
    re.compile(rf"({IDX_CHARS}{{2,80}}?)(?:（[^）]*）)*(?:以下)?「対象(?:インデックス|指数)」"),
    # ベンチマーク系
    re.compile(rf"({IDX_CHARS}{{2,80}}?)をベンチマーク"),
    re.compile(rf"ベンチマーク[はとする：:]{{0,3}}({IDX_CHARS}{{2,80}}?)(?:とします|です|。)"),
]

def clean_index_name(raw: str) -> str:
    s = raw.strip("　 、。・")
    # 先頭の助詞・接続の混入を除去（「は」「を」「、」など）
    s = re.sub(r"^(は|を|に|の|、|，|および|又は|または)+", "", s)
    return s.strip()
```

- **非貪欲（`?`）**にして、直後の動詞句の直前で止める。
- ヒット候補が複数出たら、指数プロバイダー辞書（§6）に**マッチする候補を優先**。
- 抽出後に「配当込み」等の括弧が本文で分離している場合は、原文の近傍を見て結合するか、少なくとも指数コア名（MSCI-KOKUSAI 等）が取れていればOKとする。

---

## 4. ファンド名からのフォールバック抽出（効果大・必ず実装）

野村のファンドは**名前に指数が埋まっている**ものが非常に多い。本文抽出に失敗しても、名前から救える。**本文抽出が空/低確度のとき、ファンド名に対して指数トークン辞書でマッチングする。**

```python
NAME_INDEX_MAP = [
    # (ファンド名に含まれるパターン, 正規化した指数名, provider)
    (r"MSCI[-\s]?KOKUSAI|MSCIコクサイ|コクサイ",            "MSCI-KOKUSAI指数",              "MSCI"),
    (r"MSCI.*ACWI|オール・?カントリー|全世界株",              "MSCI ACWI",                     "MSCI"),
    (r"MSCI.*エマージング|新興国株.*MSCI",                    "MSCI エマージング・マーケット",  "MSCI"),
    (r"MSCI.*インド",                                        "MSCI インディア",               "MSCI"),
    (r"MSCI.*ジャパン|MSCIジャパン",                          "MSCI ジャパン",                 "MSCI"),
    (r"TOPIX|東証株価指数",                                  "TOPIX",                         "JPX/東証"),
    (r"日経(平均|225)|日経２２５",                            "日経225",                       "日本経済新聞社"),
    (r"JPX日経(インデックス)?400",                            "JPX日経インデックス400",        "JPX/日経"),
    (r"東証REIT|J-?REIT",                                    "東証REIT指数",                  "JPX/東証"),
    (r"NASDAQ[-\s]?100|ナスダック100",                       "NASDAQ-100",                    "Nasdaq"),
    (r"S&P\s?500|S＆P500",                                   "S&P 500",                       "S&P DJI"),
    (r"NOMURA-?BPI|野村BPI",                                 "NOMURA-BPI総合",                "野村"),
    (r"FTSE.*世界国債|世界国債インデックス",                  "FTSE世界国債インデックス",      "FTSE Russell"),
    (r"Russell/?Nomura|ラッセル野村",                        "Russell/Nomura",                "FTSE Russell / 野村"),
]
```

- 名前ヒットで埋めた場合は `extraction_method="name_fallback"`, `confidence="medium"` とし、`note` に「本文未取得のため名称から推定」と記録。**創作ではなく名称に含まれる指数の転記なので許容**するが、レビュー対象にする。
- 「NEXT FUNDS ○○連動型上場投信」「野村インデックスファンド・○○（○○）」は名前だけでほぼ確定できる。

---

## 5. OCRの強制トリガー（インデックスなのに本文が取れない時）

野村の「ファンドの目的・特色」ページは**図版（インフォグラフィック）で指数名が画像化**されていることがある。周辺のボイラープレートは埋め込みテキストで取れるため「テキストは十分ある」と誤判定し、肝心の指数ボックスだけ欠落 → 不明、というパターンがある。

対策：**「インデックス型らしいのに指数が抽出できない」場合は、テキスト量に関係なくOCRを走らせる。**

インデックス型らしさの判定（いずれか）：
- ファンド名に `インデックス|連動|ETF|上場投信|NEXT FUNDS` を含む
- 本文に「連動」「対象インデックス」の語はあるが指数名の切り出しに失敗
- `fund_type` 一次判定がインデックス

該当かつ指数未取得 → 先頭3ページ（目的・特色が載る範囲）を 300dpi 以上でOCR（`pytesseract lang="jpn"`）して再抽出。

---

## 6. 指数プロバイダー判定（拡充）

抽出した指数名 or ファンド名に対して適用。§4 の `NAME_INDEX_MAP` と共通化してよい。

| キーワード（部分一致・大小/全半角無視） | provider | is_msci |
|---|---|---|
| MSCI / コクサイ / KOKUSAI / ACWI / エマージング(MSCI文脈) | MSCI | ✔ |
| TOPIX / 東証株価指数 / 東証REIT / 東証◯◯業 | JPX/東証 | |
| JPX日経(400) | JPX/日経 | |
| 日経 / 日経平均 / 日経225 / 日経半導体 / 日経高配当 | 日本経済新聞社 | |
| NOMURA-BPI / 野村(作成指数) | 野村 | |
| FTSE / Russell / ラッセル | FTSE Russell | |
| S&P / ダウ | S&P DJI | |
| NASDAQ / ナスダック | Nasdaq | |
| 記載なし・BMなし | なし | |

判定は正規化してから（全角→半角、大文字化、空白・中黒除去）行うとヒット率が上がる。

---

## 7. 処理順（改善後の抽出フロー）

```
1. section_text = 「ファンドの目的」「ファンドの特色」節を抽出（無ければ先頭3ページ全文）
2. 否定文チェック → あれば benchmark="なし", return
3. §2 の全パターンで指数名候補を抽出
4. 候補を §6 辞書に照合、プロバイダー確定
   → 取れれば confidence=high, extraction_method="rule"
5. 取れない/曖昧 → LLM抽出（section_text + 先頭3ページ + ファンド名を渡す。§8）
   → 取れれば confidence=medium/high, extraction_method="llm"
6. まだ取れない & インデックス型らしい → OCR強制（§5）して 3〜5 を再実行
7. それでも取れない → §4 ファンド名フォールバック
   → 取れれば confidence=medium, extraction_method="name_fallback", needs_review=True
8. 最終的に取れなければ benchmark=null, confidence=low, needs_review=True, note に状況記録
```

**「不明」で終わらせる前に、必ず LLM → OCR → 名称フォールバックの3段を通す**のが肝。

---

## 8. LLM抽出プロンプト（改善版の指示文）

LLMには「ベンチマーク」ではなく「**この投資信託が連動をめざす／基準とする指数**」を聞く。以下をシステム/ユーザ指示に含める：

- 「インデックスファンドは『○○に連動する投資成果をめざします』と書き、"ベンチマーク" という語を使わないことが多い。**"ベンチマーク" という語の有無に関わらず、連動対象・対象インデックス・基準指数を特定せよ。**」
- 「指数名は『（配当込み）』『（円換算ベース）』などの括弧修飾を含めて正確に。」
- 「複数のマザーファンド／複合配分の場合は各資産クラスの指数と比率を配列で返す（例：マイバランス70）。」
- 「本文に指数の記載が本当に無い場合のみ null。**本文にない指数名を創作しない。**」
- 「参考指数（連動対象ではないが比較用）は benchmark ではなく reference_index に入れる。」
- 出力は**JSONのみ**。`{benchmark, benchmark_detail, index_provider, is_msci, reference_index, fund_type, confidence, note}`。

---

## 9. 追加のテストケース（回帰確認）

改善後、以下が「不明」にならないこと：

| ファンド類型 | 期待 |
|---|---|
| 「MSCIコクサイに連動する投資成果をめざす」インデックス型 | benchmark=MSCI-KOKUSAI, is_msci=✔, "ベンチマーク"語が無くても取れる |
| NEXT FUNDS TOPIX連動型上場投信 | 名称フォールバックでも確定 |
| 野村インデックスファンド・外国株式（MSCI-KOKUSAI） | 名称の括弧内から確定 |
| 図版で指数名が画像化されたインデックス型 | OCR強制で取得 |
| マイバランス70（確定拠出年金向け） | 複合BMを benchmark_detail に構造化 |
| BMなしアクティブ | 否定文検出で benchmark="なし"（null と区別） |

---

## 10. デバッグの勘所

「不明」の行を洗い出し、以下を切り分ける：
1. **テキストは取れているか？** `data/text/{code}.txt` に指数名の文字列が存在するか grep。存在するのに不明 → 抽出パターンの問題（§2/§3）。存在しない → テキスト化/OCRの問題（§5）。
2. **どの段で落ちたか** を `extraction_method` とログで確認。rule で落ちた率が高ければパターン不足、OCR未起動が多ければ §5 のトリガー条件を緩める。
3. まずは「不明」20件をサンプルに、上記1で二分するのが最短。

> この20件のサンプル（ファンド名＋抽出テキスト or PDF）を共有してもらえれば、実際の文面に合わせて正規表現とプロンプトを具体的にチューニングできる。
